- name: playing with loops
  hosts: local
  connection: local
  become: true
  tasks:
  - name: install packages
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loop:
      - nginx
      - apache
      - mysql