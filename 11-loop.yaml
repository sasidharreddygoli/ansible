- name: play with loops
  hosts: local
  connection: local
  tasks:
  - name: print the variable
    ansible.builtin.debug:
      msg: "My course is {{ item }}"
    loop:
      - Devops
      - saredbnjdnc