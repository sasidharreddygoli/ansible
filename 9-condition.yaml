- name: play with condition
  hosts: local
  connection: local
  vars:
   number: 5
  tasks:
  - name: print the number
    ansible.builtin.debug:
      msg: "The number is {{ number }}"
    when: number > 8
    